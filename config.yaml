# config.yaml
llm:
  # 从 LLM_API_KEY 环境变量读取，或在此处填写 API Key
  api_key: ""
  base_url: "https://dashscope.aliyuncs.com/compatible-mode/v1"
  model: "qwen-max"
  max_iterations: 20
  temperature: 0.7

display:
  thinking_collapsed: true
  log_level: "verbose"

tools:
  - name: bash
    enabled: true
    timeout: 30

mcp:
  servers: {}